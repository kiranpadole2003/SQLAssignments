CREATE DATABASE COMPANYDB;

USE COMPANYDB;

CREATE TABLE DEPT (
    DEPTNO INT PRIMARY KEY,
    DNAME VARCHAR(50),
    LOC VARCHAR(50)
);

INSERT INTO DEPT (DEPTNO, DNAME, LOC) VALUES
(10, 'ACCOUNTING', 'NEW YORK'),
(20, 'RESEARCH', 'DALLAS'),
(30, 'SALES', 'CHICAGO'),
(40, 'OPERATIONS', 'BOSTON');

SELECT * FROM DEPT;

CREATE TABLE EMP (
    EMPNO INT PRIMARY KEY,
    ENAME VARCHAR(50),
    JOB VARCHAR(50),
    MGR INT,
    HIREDATE DATE,
    SAL DECIMAL(10, 2),
    COMM DECIMAL(10, 2),
    DEPTNO INT,
    FOREIGN KEY (DEPTNO) REFERENCES DEPT(DEPTNO)
);

INSERT INTO EMP (EMPNO, ENAME, JOB, MGR, HIREDATE, SAL, COMM, DEPTNO) VALUES
(7369, 'SMITH',  'CLERK',    7902, '1980-12-17',  800,   NULL, 20),
(7499, 'ALLEN',  'SALESMAN', 7698, '1981-02-20', 1600,    300, 30),
(7521, 'WARD',   'SALESMAN', 7698, '1981-02-22', 1250,    500, 30),
(7566, 'JONES',  'MANAGER',  7839, '1981-04-02', 2975,   NULL, 20),
(7654, 'MARTIN', 'SALESMAN', 7698, '1981-09-28', 1250,   1400, 30),
(7698, 'BLAKE',  'MANAGER',  7839, '1981-05-01', 2850,   NULL, 30),
(7782, 'CLARK',  'MANAGER',  7839, '1981-06-09', 2450,   NULL, 30),
(7788, 'SCOTT',  'ANALYST',  7566, '1982-12-09', 3000,   NULL, 20),
(7839, 'KING',   'PRESIDENT',NULL, '1981-11-17', 5000,   NULL, 10),
(7844, 'TURNER', 'SALESMAN', 7698, '1981-09-08', 1500,      0, 30),
(7876, 'ADAMS',  'CLERK',    7788, '1983-01-12', 1100,   NULL, 20),
(7900, 'JAMES',  'CLERK',    7698, '1981-12-03',  950,   NULL, 30),
(7902, 'FORD',   'ANALYST',  7566, '1981-12-03', 3000,   NULL, 20),
(7934, 'MILLER', 'CLERK',    7782, '1982-01-23', 1300,   NULL, 10);

#1
SELECT * FROM EMP;

#2
SELECT * FROM DEPT;

#3
SELECT EMPNO, ENAME, JOB, MGR, HIREDATE
FROM EMP 
WHERE DEPTNO = '10';

#4
SELECT ENAME, SAL
FROM EMP 
WHERE JOB = 'CLERK';

#5
SELECT DEPTNO, DNAME
FROM DEPT
WHERE DEPTNO >= 20; 

#6
SELECT ENAME
FROM EMP
WHERE SAL < 2500;

#7
SELECT ENAME, SAL, COMM
FROM EMP 
WHERE COMM > SAL;

#8
SELECT EMPNO, ENAME
FROM EMP 
WHERE JOB = 'PRESIDENT';

#9
SELECT *
FROM EMP 
WHERE COMM IS NULL;

#10
SELECT * FROM EMP 
WHERE DEPTNO = '10' AND ENAME <> 'KING';

#11
SELECT ENAME
FROM EMP 
WHERE JOB IN ('ANALYST', 'CLERK');

#12
SELECT DISTINCT JOB
FROM EMP;

#13
SELECT ENAME
FROM EMP
WHERE LENGTH(ENAME) = 4;

#14
SELECT ENAME
FROM EMP 
WHERE ENAME LIKE '%r';

#15
SELECT ENAME
FROM EMP 
WHERE ENAME LIKE 'B%' OR ENAME LIKE 'M%';

#16
set sql_safe_updates=0;

UPDATE EMP
SET ENAME = 'HENRY',
    EMPNO = 7333,
    HIREDATE = '1985-12-09'
WHERE ENAME = 'TURNER';

#17
SELECT CONCAT(ENAME, '-', JOB) AS 'Employee-job'
FROM EMP
WHERE DEPTNO = '20';

#18
SELECT DISTINCT LENGTH(ENAME)
FROM EMP;

#19
SELECT AVG(SAL) AS AVERAGE_EARNING
FROM EMP 
WHERE DEPTNO = 30;

#20
SELECT * FROM EMP 
ORDER BY HIREDATE DESC
LIMIT 1;

#21 
SELECT DEPTNO, SUM(SAL) AS TOTAL_SALARY
FROM EMP
GROUP BY DEPTNO;

#22
SELECT ENAME, date_format(HIREDATE, '%d.%m.%y') AS 'HIREDATE'
FROM EMP 
WHERE DEPTNO = 20;

#23
SELECT ROUND(TIMESTAMPDIFF(MONTH, HIREDATE, CURDATE())) AS TOTAL_MONTH
FROM EMP 
WHERE JOB = 'PRESIDENT';

#24
SELECT DAYNAME(HIREDATE) AS DAYNAME 
FROM EMP 
WHERE ENAME = 'SMITH';

#25
SELECT DATE_FORMAT(HIREDATE, '%H:%i:%s') AS JOIN_TIME
FROM EMP 
WHERE ENAME = 'FORD';

#26
SELECT DAYNAME(HIREDATE) AS 'DAY'
FROM EMP 
WHERE ENAME = 'JAMES';

#27
SELECT ENAME, HIREDATE, quarter(HIREDATE) AS QUARTER_DATE
FROM EMP;

#28
SELECT ENAME, DEPTNO
FROM EMP 
WHERE MONTH(HIREDATE) NOT IN (1,2,3);






